COLLAB REMOTE - FIXED
1. py -m pip install -r requirements.txt
2. py app.py
3. open http://127.0.0.1:5000
در این نسخه دکمه ساخت اتاق مستقل از اتصال Socket.IO صفحه اتاق را باز می‌کند.
برای اتصال دو کامپیوتر از اینترنت، برنامه را روی سرور عمومی HTTPS اجرا کنید؛ در برخی شبکه‌ها TURN لازم است.
